<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.hashtag">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:icon,android:theme,android:allowBackup">
        <activity android:name=".ProjectResultActivity"></activity>
        <activity android:name=".CreateProjectActivity" />
        <activity android:name=".ProjectHouseActivity" />
        <activity android:name=".UserHomeActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".BarchartDailyActivity" />
        <activity android:name=".LoadingActivity" />
        <activity android:name=".BarchartMonthlyActivity" />
        <activity android:name=".BarchartWeeklyActivity" />
        <activity android:name=".BarChartActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>